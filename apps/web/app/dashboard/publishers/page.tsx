import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function PublishersPage() {
  return (
    <div>
      <h2 className="mb-4 text-3xl font-semibold">Publishers</h2>
      <Card>
        <CardHeader>
          <CardTitle>Publishers (placeholder)</CardTitle>
        </CardHeader>
        <CardContent>Manage publishers here.</CardContent>
      </Card>
    </div>
  );
}
